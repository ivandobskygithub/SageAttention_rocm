# Requirements for SageAttention ROCm

# Core dependencies
torch>=2.0.0
numpy>=1.19.0

# Triton for ROCm (try multiple sources)
triton>=2.0.0

# Optional but recommended
einops>=0.6.0
packaging>=20.0

# For benchmarking (optional)
matplotlib>=3.3.0
pandas>=1.3.0

# For testing (optional)
pytest>=6.0.0
pytest-benchmark>=3.4.0